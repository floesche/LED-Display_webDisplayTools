# Controlled vocabulary — fly genotypes for the Arena Studio Run-details panel.
# Free text is still allowed (datalist, not a locked select). Add an entry via a
# PR — Studio's "Save as Pull Request" can append here for you.
format_version: "1.0"
genotypes:
  - "w;UAS-CsChrimson"
  - "w;+;GMR-Gal4"
  - "Canton-S"
  - "w1118"
